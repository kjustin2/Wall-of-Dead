# Generated by the QA harnesses — evidence lives on disk, not in git.
cycles/
maps/
scenarios/
perf/
diagnostics/
progress.json
LATEST.md

# ...but the perf-baseline IS tracked (it's how regressions are detected).
!perf-baseline.json
